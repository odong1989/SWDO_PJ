select
  m.member_id
from
  member m, groupjoin j
where
  m.member_birthday between sysdate-7 and sysdate
and
  j.group_no = #{group_no}

	
1. model.add("msg", member_id + "님의 생일이 7일 남았습니다.");
	
2. if (birthlist != null) {
	dao.addbirthalert(birthlist);
	msg == member_id + "님의 생일이 7일 남았습니다.";
(query) -> insert into alert(invite_receiver, invite_content) values
	(받는사람, msg)
	}

로그인후 -> alert테이블에서 읽지 않은(check가 0인) data를 select해옴
	종 아이콘을 클릭했을 때 select정보를 보여줌



if (returnType != 0) {
 model.addattr("birthday", list)
}